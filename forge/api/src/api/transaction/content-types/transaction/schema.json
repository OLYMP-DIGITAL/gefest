{
  "kind": "collectionType",
  "collectionName": "transactions",
  "info": {
    "singularName": "transaction",
    "pluralName": "transactions",
    "displayName": "Транзакции (DEV)",
    "description": ""
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "value": {
      "type": "decimal",
      "required": true,
      "min": 0,
      "max": 5000000
    },
    "currency": {
      "type": "enumeration",
      "enum": [
        "RUB",
        "USD"
      ],
      "required": true
    },
    "payment": {
      "type": "string",
      "required": true
    },
    "confirmation_url": {
      "type": "string"
    },
    "status": {
      "type": "enumeration",
      "enum": [
        "pending",
        "waiting_for_capture",
        "succeeded",
        "canceled"
      ]
    },
    "user": {
      "type": "relation",
      "relation": "oneToOne",
      "target": "plugin::users-permissions.user"
    }
  }
}
